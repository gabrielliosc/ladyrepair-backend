import { Chat } from "../interfaces/tables.ts"

class ChatController {
    create(req: Chat, res: Response){
        //Criar uma mensagem entre cliente e prestadora
    }
    index(){
        //Retornar todas as mensagens entre cliente e prestadora
    }
}

export { ChatController }